<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Barsel Event" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/images/barsel-event.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/images/barsel-event.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }
      .navbar {
        background-color: #343a40;
        color: white;
        padding: 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .navbar-brand {
        color: white;
        font-size: 1.5rem;
        font-weight: bold;
        text-decoration: none;
        display: flex;
        align-items: center;
      }
      .navbar-brand img {
        height: 40px;
        margin-right: 10px;
      }
      .navbar-nav {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
      }
      .nav-item {
        margin-left: 1rem;
      }
      .nav-link {
        color: rgba(255,255,255,0.8);
        text-decoration: none;
        padding: 0.5rem;
        border-radius: 4px;
      }
      .nav-link:hover {
        color: white;
        background-color: rgba(255,255,255,0.1);
      }
      .container {
        padding: 1rem;
        max-width: 1200px;
        margin: 0 auto;
        flex: 1;
      }
      .button-primary, .button-secondary {
        display: inline-block;
        padding: 0.5rem 1rem;
        border-radius: 4px;
        text-decoration: none;
        cursor: pointer;
        border: none;
      }
      .button-primary {
        background-color: #007bff;
        color: white;
      }
      .button-secondary {
        background-color: #6c757d;
        color: white;
      }
      .footer {
        background-color: #343a40;
        color: white;
        padding: 2rem 0;
        margin-top: auto;
      }
      .footer-content {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        padding: 0 1rem;
      }
      .footer-section {
        margin-bottom: 1.5rem;
        flex: 1;
        min-width: 250px;
        padding-right: 2rem;
      }
      .footer-section h3 {
        font-size: 1.2rem;
        margin-bottom: 1rem;
        color: #fff;
      }
      .footer-section p {
        color: rgba(255,255,255,0.7);
        line-height: 1.5;
        margin-bottom: 0.5rem;
      }
      .footer-links {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      .footer-links li {
        margin-bottom: 0.5rem;
      }
      .footer-links a {
        color: rgba(255,255,255,0.7);
        text-decoration: none;
        transition: color 0.3s;
      }
      .footer-links a:hover {
        color: white;
        text-decoration: underline;
      }
      .footer-bottom {
        text-align: center;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255,255,255,0.1);
        margin-top: 1.5rem;
        width: 100%;
        color: rgba(255,255,255,0.5);
        font-size: 0.9rem;
      }
      @media (max-width: 768px) {
        .footer-content {
          flex-direction: column;
        }
        .footer-section {
          padding-right: 0;
        }
      }
    </style>
    
    <%= yield :styles %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# Vite React integration - only load when needed %>
    <%= yield :vite_tags %>
  </head>

  <body>
    <nav class="navbar">
      <a href="<%= root_path %>" class="navbar-brand">
        <img src="/images/barsel-logo.png" alt="Barsel Event Logo" style="height: 40px; margin-right: 10px;">
        Barsel Event App
      </a>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a href="<%= root_path %>" class="nav-link">Events</a>
        </li>
        <% if admin_signed_in? %>
          <li class="nav-item">
            <a href="<%= admin_panel_dashboard_path %>" class="nav-link">Admin Dashboard</a>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", admin_panel_logout_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <a href="<%= admin_panel_login_path %>" class="nav-link">Admin Login</a>
          </li>
        <% end %>
      </ul>
    </nav>
    
    <div class="container">
      <% if flash[:notice] %>
        <div style="background-color: #d4edda; color: #155724; padding: 1rem; margin-bottom: 1rem; border-radius: 4px;">
          <%= flash[:notice] %>
        </div>
      <% end %>
      
      <% if flash[:alert] %>
        <div style="background-color: #f8d7da; color: #721c24; padding: 1rem; margin-bottom: 1rem; border-radius: 4px;">
          <%= flash[:alert] %>
        </div>
      <% end %>
      
      <%= yield %>
    </div>

    <footer class="footer">
      <div class="footer-content">
        <div class="footer-section">
          <h3>Tentang Barsel Event</h3>
          <p>Aplikasi Barsel Event adalah platform pengelolaan acara dan undian untuk Kabupaten Barito Selatan.</p>
          <p>Kami menyediakan solusi lengkap untuk pengelolaan registrasi peserta, pengundian hadiah, dan pelaporan.</p>
        </div>
        
        <div class="footer-section">
          <h3>Links</h3>
          <ul class="footer-links">
            <li><a href="<%= root_path %>">Beranda</a></li>
            <li><a href="#">Tentang Kami</a></li>
            <li><a href="#">Bantuan</a></li>
            <li><a href="#">Kebijakan Privasi</a></li>
          </ul>
        </div>
        
        <div class="footer-section">
          <h3>Kontak</h3>
          <p>Jl. Ahmad Yani No. 10, Buntok</p>
          <p>Kabupaten Barito Selatan, Kalimantan Tengah</p>
          <p>Email: info@barsel.go.id</p>
          <p>Phone: (0532) 21018</p>
        </div>
      </div>
      
      <div class="footer-bottom">
        <p>&copy; <%= Date.current.year %> Barsel Event App. Hak Cipta Dilindungi.</p>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <%= yield :scripts %>
  </body>
</html>
